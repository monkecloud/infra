apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Never applied directly — every overlay sets `namespace:`, which renames the Namespace
# object and stamps the rest into it. See ../README.md.
resources:
  - namespace.yaml
  - rbac.yaml
  - limits.yaml
  - networkpolicies.yaml
